/**
 * Класс заглушка
 */
class Plug {
    /**
     * Вернет имя класса
     * @param {NameClass} nameClass имя класса
     */
    imNameClass(nameClass) {
        // вернуть имя класса
    }
    /**
     * Вернет выполненное действие
     * @param {String} action 
     */
    iveDoneAction(action) {
        // вернуть действие action
    }
}